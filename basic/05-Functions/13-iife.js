// Create an IIFE that creates a private variable message with the value "Hello JavaScript" and immediately prints it to the console.

(function () {
    let message = "Hello JavaScript"
    console.log(message);
})();