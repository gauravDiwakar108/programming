// Create a function calculateArea that accepts length and width as parameters and returns the area of a rectangle.

const calculateArea = (length, width) => {
    return length * width;
}

let result = calculateArea(2, 2);
console.log(`Area of Rectangle -> ${result}`);