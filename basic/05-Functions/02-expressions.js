// Create a function expression named calculateSquare that accepts a number and returns its square.

const calculateSquare = function(number){
    return number * number;
}

let result = calculateSquare(2);
console.log(`Square of 2 -> ${result}`);